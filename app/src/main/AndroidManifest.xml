<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.scooltracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ForgotPassActivity" />
        <activity android:name=".ParentActivity" />
        <activity android:name=".DriverActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Teacher.Teach_attendance_Activity" />
        <activity android:name=".Teacher.Teach_message_Activity" />
        <activity android:name=".Teacher.Teach_sentBoxActivity" />
        <activity android:name=".Teacher.Teach_MsgDetails_Activity"></activity>
    </application>

</manifest>